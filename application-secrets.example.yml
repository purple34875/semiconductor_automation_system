# TODO(user): Copy this file to application-secrets.yml in the project root.
# TODO(user): Replace all dummy values below with your real local settings.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project_DB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: app_readonly
    password: change_me
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: exaone3.5:latest
        options:
          temperature: 0.1
          num-predict: 512

server:
  port: 8080
